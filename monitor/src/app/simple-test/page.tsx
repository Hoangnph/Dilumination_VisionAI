export default function SimpleTestPage() {
  return (
    <div style={{ padding: '20px', color: '#fff', backgroundColor: '#0a0a0a', minHeight: '100vh' }}>
      <h1>Simple Static Test Page</h1>
      <p>This is a server-side rendered page with no client-side JavaScript.</p>
      <p>If you can see this, the Next.js app is working correctly.</p>
    </div>
  );
}
